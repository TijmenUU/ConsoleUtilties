#pragma once
#include <Windows.h>

COORD operator -(const COORD a, const COORD b);

COORD operator +(const COORD a, const COORD b);

COORD operator *(const COORD a, const COORD b);

COORD operator /(const COORD a, const COORD b);